import TwoPi from '../twoPi'
import Vault from '../vault'
import { BigNumberish } from 'ethers'

const curveApy = async (twoPi: TwoPi, vault: Vault): Promise<number> => {
  throw Error('To be implemented')
}

export default curveApy
